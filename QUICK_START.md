# 🚀 Быстрый старт с авторизацией

## Шаг 1: Установите зависимости (если еще не установлены)

```bash
npm install
```

## Шаг 2: Настройте MongoDB

### Получите Connection String из MongoDB Compass:

1. Откройте MongoDB Compass
2. Найдите ваш connection string (обычно вверху или в настройках подключения)
3. Формат: `mongodb+srv://username:password@shymkent.fqeqszf.mongodb.net/`

### Или из MongoDB Atlas:

1. Откройте https://cloud.mongodb.com
2. Войдите в аккаунт
3. Выберите кластер `shymkent.fqeqszf.mongodb.net`
4. Нажмите "Connect" → "Drivers" → "Node.js"
5. Скопируйте Connection String

## Шаг 3: Создайте файл `.env.local`

В корне проекта создайте файл `.env.local`:

```env
# OpenAI API Key
OPENAI_API_KEY=ваш_ключ_openai

# MongoDB Connection String
# Замените username и password на ваши данные
MONGODB_URI=mongodb+srv://username:password@shymkent.fqeqszf.mongodb.net/?retryWrites=true&w=majority
MONGODB_DB=sayahat

# NextAuth Secret (сгенерируйте случайную строку минимум 32 символа)
# Windows PowerShell: [Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Maximum 256 }))
# Linux/Mac: openssl rand -base64 32
NEXTAUTH_SECRET=ваш-случайный-секретный-ключ-минимум-32-символа
NEXTAUTH_URL=http://localhost:3000
```

## Шаг 4: Генерация NEXTAUTH_SECRET

Выполните в терминале:

**Windows PowerShell:**
```powershell
[Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Maximum 256 }))
```

**Linux/Mac:**
```bash
openssl rand -base64 32
```

**Или просто используйте любой длинный случайный текст** (минимум 32 символа)

## Шаг 5: Перезапустите сервер

```bash
npm run dev
```

## Шаг 6: Зарегистрируйтесь

1. Откройте http://localhost:3000
2. Нажмите "Регистрация" в шапке сайта
3. Заполните форму:
   - Имя
   - Email
   - Пароль (минимум 6 символов)
4. После регистрации вы будете перенаправлены на страницу входа

## Шаг 7: Войдите в систему

1. Введите email и пароль
2. После входа вы сможете:
   - Создавать планы и заметки вручную
   - Сохранять планы и заметки через AI-чат
   - Видеть только свои данные

## 📋 Что будет в MongoDB

После регистрации и использования будут созданы коллекции:

- **`users`** - пользователи (email, хешированный пароль, имя)
- **`plans`** - планы путешествий (привязаны к userId)
- **`notes`** - заметки (привязаны к userId)
- **`places`** - места/локации (общие для всех)
- **`towns`** - города (уже существует)

## ✅ Проверка

1. Зарегистрируйтесь и войдите
2. Создайте план или заметку
3. Откройте MongoDB Compass
4. Проверьте коллекции `plans` или `notes`
5. Убедитесь, что у каждого документа есть поле `userId`

## 🔧 Если что-то не работает

1. **Проверьте `.env.local`** - все переменные должны быть заполнены
2. **Проверьте консоль сервера** - там будут ошибки подключения
3. **Проверьте MongoDB Connection String** - должен быть правильный формат
4. **Перезапустите сервер** после изменения `.env.local`

Подробная инструкция: `MONGODB_AUTH_SETUP.md`

