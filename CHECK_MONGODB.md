# 🔍 Проверка подключения MongoDB

## Шаг 1: Проверьте файл `.env.local`

Убедитесь, что файл `.env.local` существует и содержит:

```env
MONGODB_URI=mongodb+srv://username:password@shymkent.fqeqszf.mongodb.net/?retryWrites=true&w=majority
MONGODB_DB=sayahat
NEXTAUTH_SECRET=ваш-секретный-ключ-минимум-32-символа
NEXTAUTH_URL=http://localhost:3000
```

## Шаг 2: Проверьте Connection String

**Важно:** Connection String должен быть правильного формата:

✅ **Правильно:**
```
mongodb+srv://username:password@shymkent.fqeqszf.mongodb.net/?retryWrites=true&w=majority
```

❌ **Неправильно:**
```
mongodb://username:password@shymkent.fqeqszf.mongodb.net  (без +srv)
mongodb+srv://shymkent.fqeqszf.mongodb.net  (без username:password)
```

## Шаг 3: Проверьте консоль сервера

1. Откройте терминал, где запущен `npm run dev`
2. Попробуйте зарегистрироваться
3. Смотрите логи - должны быть сообщения:
   - `[Register] Starting registration process...`
   - `[Register] Connecting to database...`
   - `[Register] Database connected successfully`
   - Или ошибки подключения

## Шаг 4: Проверьте MongoDB Compass

1. Откройте MongoDB Compass
2. Убедитесь, что подключение работает
3. Проверьте, что база данных `sayahat` существует
4. Проверьте коллекцию `users` - должны быть пользователи

## Шаг 5: Типичные ошибки

### Ошибка: "Missing MongoDB credentials"
**Решение:** Проверьте, что в `.env.local` есть `MONGODB_URI` и `MONGODB_DB`

### Ошибка: "MongoDB connection timeout"
**Решение:** 
- Проверьте правильность Connection String
- Проверьте, что MongoDB доступна
- Проверьте Network Access в MongoDB Atlas (должен быть разрешен доступ)

### Ошибка: "Authentication failed"
**Решение:**
- Проверьте правильность username и password в Connection String
- Убедитесь, что пароль не содержит неэкранированные специальные символы

### Ошибка: "User with this email already exists"
**Решение:** Пользователь с таким email уже зарегистрирован. Используйте другой email или войдите.

## Шаг 6: Как получить правильный Connection String

### Из MongoDB Compass:
1. Откройте MongoDB Compass
2. Посмотрите на строку подключения вверху
3. Или создайте новое подключение и скопируйте connection string

### Из MongoDB Atlas:
1. Откройте https://cloud.mongodb.com
2. Войдите в аккаунт
3. Выберите кластер `shymkent.fqeqszf.mongodb.net`
4. Нажмите "Connect" → "Drivers" → "Node.js"
5. Скопируйте Connection String
6. Замените `<password>` на ваш реальный пароль

## Шаг 7: Проверка работы

1. Откройте http://localhost:3000/auth/signup
2. Заполните форму регистрации
3. Откройте консоль браузера (F12 → Console)
4. Откройте консоль сервера (терминал)
5. Смотрите логи - должны быть сообщения о процессе регистрации

Если видите ошибки - скопируйте их и проверьте по списку выше.

