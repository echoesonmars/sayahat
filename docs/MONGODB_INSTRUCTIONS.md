# 📝 Инструкция: Что нужно сделать в MongoDB

## ✅ Шаг 1: Получите Connection String

### Из MongoDB Compass:
1. Откройте MongoDB Compass
2. Посмотрите на строку подключения вверху или в настройках
3. Скопируйте её

### Или из MongoDB Atlas (если используете облако):
1. Откройте https://cloud.mongodb.com
2. Войдите в аккаунт
3. Выберите ваш кластер (shymkent.fqeqszf.mongodb.net)
4. Нажмите "Connect" → "Drivers" → "Node.js"
5. Скопируйте Connection String

**Формат будет примерно таким:**
```
mongodb+srv://username:password@shymkent.fqeqszf.mongodb.net/?retryWrites=true&w=majority
```

## ✅ Шаг 2: Настройте файл `.env.local`

1. Откройте файл `.env.local` в корне проекта (или создайте его, скопировав `env.example`)

2. Добавьте следующие строки:

```env
# OpenAI (если еще не добавлен)
OPENAI_API_KEY=ваш_ключ_openai

# MongoDB - замените username и password на ваши данные
MONGODB_URI=mongodb+srv://username:password@shymkent.fqeqszf.mongodb.net/?retryWrites=true&w=majority
MONGODB_DB=sayahat

# NextAuth Secret - сгенерируйте случайную строку (минимум 32 символа)
# Windows PowerShell: [Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Maximum 256 }))
# Linux/Mac: openssl rand -base64 32
NEXTAUTH_SECRET=ваш-случайный-секретный-ключ-минимум-32-символа
NEXTAUTH_URL=http://localhost:3000
```

**Важно:**
- Замените `username` и `password` на ваши реальные данные из MongoDB
- Если пароль содержит специальные символы (@, #, $), закодируйте их:
  - `@` → `%40`
  - `#` → `%23`
  - `$` → `%24`

## ✅ Шаг 3: Сгенерируйте NEXTAUTH_SECRET

**Windows PowerShell:**
```powershell
[Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Maximum 256 }))
```

**Linux/Mac:**
```bash
openssl rand -base64 32
```

**Или просто используйте любой длинный случайный текст** (минимум 32 символа)

Скопируйте результат и вставьте в `.env.local` как значение `NEXTAUTH_SECRET`

## ✅ Шаг 4: Перезапустите сервер

```bash
# Остановите сервер (Ctrl+C) и запустите снова:
npm run dev
```

## ✅ Шаг 5: Проверьте работу

1. Откройте http://localhost:3000
2. Нажмите "Регистрация" в шапке
3. Создайте аккаунт
4. Войдите в систему
5. Создайте план или заметку
6. Проверьте в MongoDB Compass - данные должны появиться в коллекциях

## 📊 Структура базы данных

После использования будут созданы коллекции:

- **`users`** - пользователи (email, хешированный пароль, имя)
- **`plans`** - планы путешествий (привязаны к userId)
- **`notes`** - заметки (привязаны к userId)
- **`places`** - места/локации (общие)
- **`towns`** - города (уже существует)

## 🔍 Проверка в MongoDB Compass

1. Откройте MongoDB Compass
2. Подключитесь к вашей базе данных
3. Выберите базу `sayahat`
4. Проверьте коллекции:
   - `users` - должны быть зарегистрированные пользователи
   - `plans` - планы с полем `userId`
   - `notes` - заметки с полем `userId`

## ⚠️ Важные моменты

1. **Безопасность:**
   - Пароли хранятся в хешированном виде (bcrypt)
   - Никогда не коммитьте `.env.local` в Git
   - `NEXTAUTH_SECRET` должен быть уникальным

2. **Данные пользователей:**
   - Каждый пользователь видит только свои планы и заметки
   - Данные изолированы по `userId`

3. **Если что-то не работает:**
   - Проверьте консоль сервера (терминал) - там будут ошибки
   - Убедитесь, что MongoDB подключена
   - Проверьте правильность Connection String

