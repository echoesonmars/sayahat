# 🔧 Исправление проблемы с регистрацией

## ✅ Что было исправлено:

1. **Добавлено детальное логирование** - теперь в консоли сервера видны все шаги регистрации
2. **Улучшена обработка ошибок** - более понятные сообщения об ошибках
3. **Исправлена фильтрация заметок** - теперь заметки фильтруются по `userId`
4. **Добавлены логи в аутентификации** - видно, что происходит при входе

## 🔍 Как проверить проблему:

### Шаг 1: Проверьте `.env.local`

Убедитесь, что файл `.env.local` существует в корне проекта и содержит:

```env
MONGODB_URI=mongodb+srv://username:password@shymkent.fqeqszf.mongodb.net/?retryWrites=true&w=majority
MONGODB_DB=sayahat
NEXTAUTH_SECRET=ваш-секретный-ключ-минимум-32-символа
NEXTAUTH_URL=http://localhost:3000
OPENAI_API_KEY=ваш-ключ-openai
```

**Важно:**
- Замените `username` и `password` на реальные данные из MongoDB
- Если пароль содержит `@`, `#`, `$` - закодируйте их:
  - `@` → `%40`
  - `#` → `%23`
  - `$` → `%24`

### Шаг 2: Перезапустите сервер

```bash
# Остановите сервер (Ctrl+C)
npm run dev
```

### Шаг 3: Попробуйте зарегистрироваться

1. Откройте http://localhost:3000/auth/signup
2. Заполните форму
3. **Откройте консоль браузера** (F12 → Console)
4. **Смотрите консоль сервера** (терминал, где запущен `npm run dev`)

### Шаг 4: Проверьте логи

В консоли сервера должны появиться сообщения:

```
[Register] Starting registration process...
[Register] Received data: { email: '...', name: '...', passwordLength: ... }
[Register] Connecting to database...
[Register] Database connected successfully
[Register] Checking if user exists...
[Register] Hashing password...
[Register] Creating user...
[Register] User created successfully with ID: ...
```

Если видите ошибки - скопируйте их.

## 🐛 Типичные ошибки:

### Ошибка: "Missing MongoDB credentials"
**Причина:** В `.env.local` нет `MONGODB_URI` или `MONGODB_DB`  
**Решение:** Добавьте эти переменные в `.env.local`

### Ошибка: "MongoDB connection timeout"
**Причина:** Неправильный Connection String или MongoDB недоступна  
**Решение:**
1. Проверьте правильность `MONGODB_URI` в `.env.local`
2. Проверьте, что MongoDB Atlas доступна (откройте в браузере)
3. Проверьте Network Access в MongoDB Atlas - должен быть разрешен доступ с вашего IP

### Ошибка: "Authentication failed"
**Причина:** Неправильный username или password в Connection String  
**Решение:**
1. Проверьте правильность username и password
2. Убедитесь, что пароль закодирован (если содержит спецсимволы)

### Ошибка: "User with this email already exists"
**Причина:** Пользователь с таким email уже зарегистрирован  
**Решение:** Используйте другой email или войдите в существующий аккаунт

### Ошибка в консоли браузера: "Failed to register user"
**Причина:** Ошибка на сервере  
**Решение:** Смотрите консоль сервера - там будет детальная информация об ошибке

## 📋 Проверка в MongoDB Compass

1. Откройте MongoDB Compass
2. Подключитесь к вашей базе данных
3. Выберите базу `sayahat`
4. Проверьте коллекцию `users` - должны быть пользователи

## 🔗 Коллекции в базе данных

Ваша база данных `sayahat` должна содержать:

- ✅ **`users`** - пользователи (создается при регистрации)
- ✅ **`plans`** - планы путешествий (создается при сохранении плана)
- ✅ **`notes`** - заметки (создается при сохранении заметки)
- ✅ **`towns`** - города (уже существует)
- ✅ **`places`** - места/локации (опционально)

## 💡 Если ничего не помогает:

1. **Проверьте Connection String в MongoDB Compass:**
   - Откройте MongoDB Compass
   - Посмотрите строку подключения
   - Скопируйте её в `.env.local` как `MONGODB_URI`

2. **Проверьте, что база данных `sayahat` существует:**
   - В MongoDB Compass должна быть база `sayahat`
   - Если её нет - создайте её

3. **Проверьте Network Access в MongoDB Atlas:**
   - Откройте https://cloud.mongodb.com
   - Network Access → Add IP Address
   - Добавьте `0.0.0.0/0` для доступа откуда угодно (или ваш IP)

4. **Скопируйте точную ошибку из консоли сервера** и проверьте по списку выше

